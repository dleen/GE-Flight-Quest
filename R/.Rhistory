source('~/Documents/Kaggle/GE Flight Quest/R/delaybyairline.R')
setwd("~/Documents/Kaggle/GE Flight Quest")
source('~/Documents/Kaggle/GE Flight Quest/R/delaybyairline.R')
setwd("~/Documents/Kaggle/GE Flight Quest/R")
source('~/Documents/Kaggle/GE Flight Quest/R/delaybyairline.R')
flightHistory$local_time_scheduled_gate_departure
wday(flightHistory$local_time_scheduled_gate_departure)
hist()
hist(wday(flightHistory$local_time_scheduled_gate_departure))
source('~/Documents/Kaggle/GE Flight Quest/R/delaybyairline.R')
hist(wday(flightHistory$local_time_scheduled_gate_departure))
barplot(wday(flightHistory$local_time_scheduled_gate_departure))
hist(wday(flightHistory$local_time_scheduled_gate_departure))
?ist
?hist
late <- flightHistory[flightHistory$gate_arrival_delay_mins > 0,]
hist(wday(late$local_time_scheduled_gate_departure))
late <- flightHistory[flightHistory$gate_arrival_delay_mins > 10,]
hist(wday(late$local_time_scheduled_gate_departure))
late <- flightHistory[flightHistory$gate_arrival_delay_mins > 59,]
hist(wday(late$local_time_scheduled_gate_departure))
source('~/Documents/Kaggle/GE Flight Quest/R/delaybyairline.R')
pwd
ls
getwd
getwd()
?write.csv
write.csv(flightHistory, file="flighthistory_full.csv")
flightHistory <- read.csv("flighthistory_full.csv")
rstudio::viewData(flightHistory)
hist(wday(flightHistory$local_time_scheduled_gate_departure))
late <- flightHistory[flightHistory$gate_arrival_delay_mins > 10,]
hist(wday(late$local_time_scheduled_gate_departure))
late <- flightHistory[flightHistory$gate_arrival_delay_mins > 59,]
hist(wday(late$local_time_scheduled_gate_departure))
hist(wday(late$local_time_actual_gate_departure))
hist(wday(late$local_time_scheduled_gate_departure))
hist(wday(late$scheduled_gate_departure))
late <- flightHistory[flightHistory$gate_arrival_delay_mins > 0,]
hist(wday(late$scheduled_gate_departure))
hist(wday(late$local_time_scheduled_gate_departure))
hist(wday(late$local_time_actual_gate_departure))
hist(wday(flightHistory$local_time_actual_gate_departure))
?count
?aggregate
install.packages("data.table")
library(data.table)
